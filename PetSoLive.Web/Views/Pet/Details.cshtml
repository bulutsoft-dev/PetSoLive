@{
    ViewData["Title"] = "Pet Details";
}
    @{
        Layout = "_Layout";
    }

    <div class="container">
    <h2 class="my-4 text-center">@Model.Name's Details</h2>

    <div class="row">
        <div class="col-md-6">
            <!-- Pet Image -->
            <img src="" class="img-fluid" alt="@Model.Name" />
        </div>
        <div class="col-md-6">
            <h3>@Model.Name</h3>
            <p><strong>Species:</strong> @Model.Species</p>
            <p><strong>Breed:</strong> @Model.Breed</p>
            <p><strong>Age:</strong> @Model.Name</p> <!-- Add more fields as needed -->
            <p><strong>Adopted:</strong> @(Model.Name)</p> <!-- Assuming you have an IsAdopted field -->
        </div>
    </div>

    <!-- Return to List Button -->
    <div class="text-right mt-4">
        <a asp-controller="Adoption" asp-action="Index" class="btn btn-secondary">Back to Adoption List</a>
    </div>
</div>