@{
ViewData["Title"] = "Error";
}

<h2>@ViewData["Title"]</h2>

@if (ViewBag.ErrorMessage != null)
{
<div class="alert alert-danger">
    <strong>Error:</strong> @ViewBag.ErrorMessage
</div>
}

<p><a href="@Url.Action("Index", "Home")">Go back to homepage</a></p>
