@{
ViewData["Title"] = "Create Lost Pet Ad";
}

<h2>Create Lost Pet Ad</h2>

<form asp-action="Create" method="post">
    @if (TempData["SuccessMessage"] != null)
    {
    <div class="alert alert-success">
        @TempData["SuccessMessage"]
    </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
    <div class="alert alert-danger">
        @TempData["ErrorMessage"]
    </div>
    }

    <div class="form-group">
        <label for="PetName">Pet Name</label>
        <input type="text" class="form-control" id="PetName" name="PetName" required />
    </div>

    <div class="form-group">
        <label for="Description">Description</label>
        <textarea class="form-control" id="Description" name="Description" rows="4" required></textarea>
    </div>

    <div class="form-group">
        <label for="LastSeenDate">Last Seen Date</label>
        <input type="date" class="form-control" id="LastSeenDate" name="LastSeenDate" required />
    </div>

    <div class="form-group">
        <label for="LastSeenCity">City</label>
        <select class="form-control" id="LastSeenCity" name="LastSeenCity" required>
            <option value="">Select City</option>
            @foreach (var city in ViewData["Cities"] as List<string>)
            {
            <option value="@city">@city</option>
            }
        </select>
    </div>

    <div class="form-group">
        <label for="LastSeenDistrict">District</label>
        <select class="form-control" id="LastSeenDistrict" name="LastSeenDistrict" required>
            <option value="">Select District</option>
        </select>
    </div>

    <div class="form-group">
        <label for="ImageUrl">Image URL (Required)</label>
        <input type="url" class="form-control" id="ImageUrl" name="ImageUrl" placeholder="Enter Image URL" />
    </div>

    <button type="submit" class="btn btn-primary">Create Lost Pet Ad</button>
</form>

@section Scripts {
<script>
    // Update districts based on selected city
    $('#LastSeenCity').change(function () {
        var city = $(this).val();
        $.get('/LostPetAd/GetDistricts?city=' + city, function (data) {
            var districtSelect = $('#LastSeenDistrict');
            districtSelect.empty();
            districtSelect.append('<option value="">Select a district</option>');
            data.forEach(function (district) {
                districtSelect.append('<option value="' + district + '">' + district + '</option>');
            });
        });
    });
</script>
}
